/*
package com.xdaocloud.futurechain.kafka;

import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;

*/
/**
 * 消息消费者
 * 
 * @author WangPengHua
 *//*

@Component
public class KafkaConsumer {

    @KafkaListener(topics = {"test", "logs"})
    public void processLogs(String content) {
        // System.out.println("消费者收到的消息:" + content);
    }

    @KafkaListener(topics = {"xdao_mcm_email", "xdao_mcm_msg", "xdao_mcm_sms"})
    public void processMessage(String content) {
        System.out.println("消费者收到的消息:-------" + content);
    }
}*/
